# TrialShield India region

Trial search is scoped to customers in India (IN). The backend requires official evidence of India availability and excludes country-only offers such as Ireland-only trials.

Set GEMINI_API_KEY in .env before starting backend.ps1.
